<section class="card mt-5">
  <div class="card-body">
    <h1 class="card-title">Following users</h1>
    <%= form_tag search_friend_path, remote: true, method: :get do %>
      <div class="form-group row">
        <div class="col-sm-8 col-md-10">
          <%= text_field_tag :friend, params[:friend], placeholder: 'User name or email', autofocus: true, class: 'form-control form-control-lg w-100' %>
        </div>
        <div class="col-sm-4 col-md-2 text-right">
          <%= button_tag type: :submit, class: 'btn btn-lg btn-warning w-100' do%>
            <%= fa_icon 'search' %> Search
          <% end %>
        </div>
      </div>
    <% end %>
    <div id="friends_results"></div>
    <% if !@friends.empty? %>
      <%= render 'friends/list' %>
    <% end %>
  </div>
</section>
